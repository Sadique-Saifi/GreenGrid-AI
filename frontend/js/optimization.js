/* ============================================================
   optimization.js — currently the Optimization page is mostly
   static markup (allocation table + why-panel + recommendations).
   TODO: fetch live allocation from GET /optimization/recommendation
   and rebuild the table + why-panel bullets from the response.
   ============================================================ */
console.log('Optimization page loaded — wire up /optimization/recommendation here.');
