/* ============================================================
   config.js — Configuration / System Info page.
   Currently static; TODO: fetch model health from a
   GET /model/health endpoint and site params from GET /config.
   ============================================================ */
console.log('Config page loaded.');
